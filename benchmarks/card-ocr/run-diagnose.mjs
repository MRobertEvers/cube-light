import{chromium}from'playwright';import{readFile,writeFile}from'node:fs/promises';
const truth=JSON.parse(await readFile(new URL('./photo-ground-truth.json',import.meta.url),'utf8'));const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});
try{const p=await b.newPage();await p.routeWebSocket('**/*',ws=>{ws.onMessage(()=>{});ws.send(JSON.stringify({type:'connected'}));});p.on('console',m=>{if(m.type()==='log')console.log(m.text())});await p.goto('http://127.0.0.1:4173/diagnose-crops.html');await p.waitForFunction(()=>typeof window.diagnoseCrops==='function');const result=await p.evaluate(boxes=>window.diagnoseCrops({boxes}),truth.cards.filter(c=>c.visibility==='full').map(({id,box})=>({id,box})));await writeFile(new URL('./photo-results/oracle-diagnostic.json',import.meta.url),JSON.stringify(result,null,2));}finally{await b.close();}
