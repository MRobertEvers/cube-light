import{chromium}from'playwright';import{writeFile}from'node:fs/promises';
const args=Object.fromEntries(process.argv.slice(2).map(x=>x.split('='))),browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH||'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});
try{const page=await browser.newPage();await page.routeWebSocket('**/*',ws=>{ws.onMessage(()=>{});ws.send(JSON.stringify({type:'connected'}));});page.on('console',m=>console.log(m.type(),m.text()));page.on('pageerror',e=>console.error(String(e)));await page.goto('http://127.0.0.1:4173/font-ocr.html');await page.waitForFunction(()=>typeof window.scanFontOCR==='function');const result=await page.evaluate(config=>window.scanFontOCR(config),{fontFile:args.font||'beleren.woff',rectify:args.rectify==='true',blur:Number(args.blur||1.3),mode:args.mode||'tiles',tileSize:Number(args.tile||1400),limit:Number(args.limit||0)});await writeFile(new URL(`./photo-results/${args.id||'font-'+(args.mode||'tiles')}.json`,import.meta.url),JSON.stringify(result,null,2));}finally{await browser.close();}
