import{chromium}from'playwright';import{writeFile}from'node:fs/promises';const args=Object.fromEntries(process.argv.slice(2).map(x=>x.split('=')));const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage();await p.routeWebSocket('**/*',ws=>{ws.onMessage(()=>{});ws.send(JSON.stringify({type:'connected'}));});p.on('pageerror',e=>console.error(String(e)));await p.goto('http://127.0.0.1:4173/refine-font.html');await p.waitForFunction(()=>typeof window.refineExistingFont==='function');const r=await p.evaluate(config=>window.refineExistingFont(config),{files:(args.files||'font-plane-v7,font-bands-v6,font-proposals-v2').split(','),broadBlur:args.broad==='true'});await writeFile(new URL('./photo-results/'+(args.id||'font-refined-v8')+'.json',import.meta.url),JSON.stringify(r,null,2));console.log('Refined',r.outputs.length);}finally{await b.close();}
