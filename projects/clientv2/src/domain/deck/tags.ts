/** Tag rules shared with the server, so a tag saved here is stored exactly as typed. */
export {
	deckInGroup,
	MAX_DECK_GROUPS,
	MAX_DECK_TAGS,
	MAX_GROUP_NAME_LENGTH,
	MAX_TAG_LENGTH,
	normalizeTag,
	normalizeTags,
	tagKey
} from '@torimtg/core';
